// Firebase placeholder - replace with your config when ready
export const initFirebase = () => {
  // import { initializeApp } from 'firebase/app'
  // const app = initializeApp({ apiKey: process.env.NEXT_PUBLIC_FIREBASE_API_KEY, ... })
  return null
}
